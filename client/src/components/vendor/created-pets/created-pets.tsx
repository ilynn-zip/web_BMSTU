import { FC } from "react";
import styles from "./created-pets.module.css";
interface CreatedPetsProps {}

const CreatedPets: FC<CreatedPetsProps> = ({}) => {
    return <div>CreatedPets</div>;
};
export { CreatedPets };
